import 'package:flutter/material.dart';

class OnbordingBigText extends StatelessWidget {
  const OnbordingBigText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
